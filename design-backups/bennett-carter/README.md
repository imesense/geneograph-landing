# Bennett–Carter landing backup

This unpublished directory preserves the illustrative human-family landing page before the switch to real Whiskerfield prototype captures. It includes its HTML, CSS, logo and portrait asset. The GitHub Pages build publishes only `src/`, so this backup is not deployed.
